replicaCount: 1

image:
  repository: tic-tac-toe-gateway
  pullPolicy: IfNotPresent
  # tag: "latest"

nameOverride: ""
fullnameOverride: ""

serviceAccount:
  create: false

podAnnotations: {}

podSecurityContext: {}

securityContext: {}

resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 100m
    memory: 128Mi

nodeSelector: {}

tolerations: []

affinity: {}

# Global config reference
globalConfig:
  name: global-config-config
  namespace: default

# Service configuration
service:
  type: ClusterIP
  port: 9000

# Ingress configuration
ingress:
  enabled: true
  className: "nginx"
  hosts:
    - host: gateway.tic-tac-toe.local
      paths:
        - path: /
          pathType: Prefix
  tls: []

