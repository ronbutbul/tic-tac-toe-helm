apiVersion: v1
kind: Pod
metadata:
  name: node-debug11
  namespace: default
spec:
  hostPID: true
  hostNetwork: true
  nodeName: i-0ee453aa6dbc75dd3   # <-- CHANGE to your GPU node name
  tolerations:
    - key: "nvidia.com/gpu"
      operator: "Exists"
      effect: "NoSchedule"
  containers:
    - name: debug
      image: ronbutbul135/alpine:3.19
      command:
        - sleep
        - infinity
      securityContext:
        privileged: true
      volumeMounts:
        - name: host-root
          mountPath: /host
  volumes:
    - name: host-root
      hostPath:
        path: /
