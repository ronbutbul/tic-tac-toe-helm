apiVersion: v1
kind: Secret
metadata:
  name: {{ include "elasticsearch-mcp.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "elasticsearch-mcp.labels" . | nindent 4 }}
type: Opaque
data:
  {{- if .Values.elasticsearch.serviceAccountToken }}
  ZIBI: {{ .Values.elasticsearch.serviceAccountToken | b64enc }}
  {{- end }}
 
