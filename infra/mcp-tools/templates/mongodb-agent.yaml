{{- if and (index .Values "mongodb-mcp" "enabled") (index .Values "agents" "mongodb-agent" "enabled") }}
apiVersion: kagent.dev/v1alpha2
kind: Agent
metadata:
  name: {{ index .Values "agents" "mongodb-agent" "name" | default "mongodb-agent" }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "mcp-tools.labels" . | nindent 4 }}
    app.kubernetes.io/component: mongodb-agent
spec:
  description: {{ index .Values "agents" "mongodb-agent" "description" | default "MongoDB assistant agent (via MCP)" }}
  type: Declarative
  declarative:
    modelConfig: {{ index .Values "agents" "mongodb-agent" "modelConfig" | default "vllm-gpt" }}
    stream: true
    {{- with index .Values "agents" "mongodb-agent" "context" }}
    context:
      {{- toYaml . | nindent 6 }}
    {{- end }}
    {{- with index .Values "agents" "mongodb-agent" "deployment" }}
    deployment:
      {{- toYaml . | nindent 6 }}
    {{- end }}
    systemMessage: |
      {{- index .Values "agents" "mongodb-agent" "systemMessage" | nindent 6 }}
    tools:
    - type: McpServer
      mcpServer:
        apiGroup: kagent.dev
        kind: RemoteMCPServer
        name: {{ include "mcp-tools.mongodb-mcp.remoteName" . }}
        toolNames:
        {{- toYaml (index .Values "agents" "mongodb-agent" "toolNames") | nindent 8 }}
{{- end }}
