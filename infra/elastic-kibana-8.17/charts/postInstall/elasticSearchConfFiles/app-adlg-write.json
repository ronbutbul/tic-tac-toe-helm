{
    "index_patterns": [ "app-adlg-write*" ],
    "data_stream": { },
    "priority": 600,
    "template": {
      "settings": {
        "index.lifecycle.name": "tsgs-policy-delete",
        "index.default_pipeline": "structured-metadata-format"
      }
    }
}
