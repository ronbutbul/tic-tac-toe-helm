{
    "index_patterns": [ "app-elastictest-write*" ],
    "data_stream": { },
    "priority": 500,
    "template": {
      "settings": {
        "number_of_shards": 4,
        "index.lifecycle.name": "test-policy-delete",
        "index.default_pipeline": "structured-metadata-format"
      }
    }
  }
