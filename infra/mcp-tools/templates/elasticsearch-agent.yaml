{{- if and (index .Values "elasticsearch-mcp" "enabled") (index .Values "agents" "elasticsearch-agent" "enabled") }}
apiVersion: kagent.dev/v1alpha2
kind: Agent
metadata:
  name: {{ index .Values "agents" "elasticsearch-agent" "name" | default "elasticsearch-agent" }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "mcp-tools.labels" . | nindent 4 }}
    app.kubernetes.io/component: elasticsearch-agent
spec:
  description: {{ index .Values "agents" "elasticsearch-agent" "description" | default "Elasticsearch / Kubernetes logs assistant for analyzing info, debug, and error logs and troubleshooting crashes (via MCP)" }}
  type: Declarative
  declarative:
    modelConfig: {{ index .Values "agents" "elasticsearch-agent" "modelConfig" | default "vllm-gpt" }}
    stream: true
    {{- with index .Values "agents" "elasticsearch-agent" "context" }}
    context:
      {{- toYaml . | nindent 6 }}
    {{- end }}
    {{- with index .Values "agents" "elasticsearch-agent" "deployment" }}
    deployment:
      {{- toYaml . | nindent 6 }}
    {{- end }}
    systemMessage: |
      {{- index .Values "agents" "elasticsearch-agent" "systemMessage" | nindent 6 }}
    tools:
    - type: McpServer
      mcpServer:
        apiGroup: kagent.dev
        kind: RemoteMCPServer
        name: {{ include "mcp-tools.elasticsearch-mcp.remoteName" . }}
        toolNames:
        {{- toYaml (index .Values "agents" "elasticsearch-agent" "toolNames") | nindent 8 }}
{{- end }}
